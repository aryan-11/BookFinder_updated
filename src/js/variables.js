const popupBtn = document.querySelector('.closePopup');

const sceneOne = document.querySelector('.one');
const sceneTwo = document.querySelector('.two');
const sceneThree = document.querySelector('.three');

const input = document.querySelector('#searchbar');
const form = document.querySelector('#searchbarForm');

const category = document.querySelector('.category');
const title = document.querySelector('.title');
const description = document.querySelector('.description');
const wrapperResults = document.querySelector('.wrapperResults');

export {popupBtn, sceneOne, sceneTwo, sceneThree, input, form, category, title, description, wrapperResults};